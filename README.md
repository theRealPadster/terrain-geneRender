# terrain-geneRender
OpenGL terrain generator and renderer project made with a friend. 

Compile with:
g++ -I /usr/include -L /usr/lib obj/Debug/main.o -lGL -lglut -lGLU -lGLEW -lX11 -lm -lrt -lpng -o main
